import 'package:flutter/material.dart';

/// Couleurs de marque de Yame — fond sombre bleu-nuit, accent jaune/or.
class AppColors {
  AppColors._();

  static const background = Color(0xFF0B1120);
  static const surface = Color(0xFF141B2E);
  static const surfaceElevated = Color(0xFF1C2440);

  static const accent = Color(0xFFFFC229);
  static const accentDark = Color(0xFFE0A800);

  static const textPrimary = Color(0xFFF5F6FA);
  static const textSecondary = Color(0xFFA9B2C3);
  static const textDisabled = Color(0xFF5C6478);

  static const border = Color(0xFF2A334D);

  static const success = Color(0xFF3DDC97);
  static const error = Color(0xFFFF6B6B);
}
